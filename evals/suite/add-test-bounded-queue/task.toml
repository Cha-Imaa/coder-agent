id = "add-test-bounded-queue"
category = "add-test"
prompt = """
Write tests/test_bounded_queue.py for BoundedQueue in bounded_queue.py. Cover FIFO order, len(), is_full() exactly at capacity, the OverflowError when pushing to a full queue, the IndexError when popping from an empty one, and the ValueError for a non-positive capacity.
"""
notes = """
Second mutation-graded task. The mutants target exactly the boundaries the prompt names, so a test file that only checks the happy path fails the grade.
"""

id = "add-feature-stack-peek"
category = "add-feature"
prompt = """
Add two things to the Stack class in stack.py: a peek() method that returns the top item without removing it and raises IndexError on an empty stack, and __len__ so len(stack) works. Add tests for both.
"""
notes = """
Smallest feature task. Checks the agent follows a precise spec (exception type) and extends the existing tests instead of only editing code.
"""

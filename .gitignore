.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
.chroma/
.coder-agent/
*.sqlite
.DS_Store

# local assistant notes
CLAUDE.md
.claude/

# coverage output (CI turns it into the README badge)
.coverage
coverage.json
htmlcov/

# diagram sources and inspiration images (exported PNGs live in docs/figures/); anchored,
# because docs/assets/ is the site's own stylesheet and script and belongs in the repository
/assets/
# local before/after comparison of the restyled figures
docs/figures/preview/

# personal roadmap and learning log, kept out of the public repo
docs/PLAN.md
docs/TEACH.md
